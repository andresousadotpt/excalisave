PORT="4000"
ALLOWED_ORIGINS="http://localhost:3000"
MAX_ROOM_SIZE="10"
# Shared secret for WebSocket auth (must match main app's COLLAB_SECRET)
# Generate with: openssl rand -hex 32
COLLAB_SECRET="generate-with-openssl-rand-hex-32"